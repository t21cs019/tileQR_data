# CHANGELOG

`tileQR_data` の版と、**後から理由を思い出せないと困る判断**の記録。

コードを読めば「何をしているか」は分かる。ここに書くのは「なぜそうしたか」と
「そうしなかった場合に何が壊れるか」に限る。数値の出典や計測の進捗は
`COVERAGE.md`（自動生成）、連結の出所は `raw/JOINS.md`（自動生成）にある。

版は `pyproject.toml` の `version` と対応する。発表・投稿の節目では
`tileQR_research` のタグ運用（例: `2026-W24`）に合わせてスナップショットを取る。

---

## 0.6.0 — 使えないデータの除外を宣言的にした（curation.yaml / running.yaml）

### raw/ を手で消す運用は成立していなかった

`TODO_REMEASURE.md` は「ノイズ・不備が見つかり `raw/` へ反映していないもの」を
挙げていたが、**それを強制する仕組みが無かった**。`raw/` は `raw_data/` から
`make assemble` で再生成できることが前提のディレクトリなので、
手で消した除外は次の assemble で黙って戻る。約束を書いた場所と、
約束を守る場所が別だった。

実害も出ていた。i7-7700 / ryzen のコールドスタート点の手直し（0.5.x 以前）は
`raw/` を直接編集したもので、`raw_data/` 側には異常値が残ったままだった。
`make assemble` を回せば 0.146 GFlops に戻る状態で、
`TODO_REMEASURE.md` には「反映済み」と書いてあった。

判断を再生成の**入力側**に移した。`curation.yaml` が
「raw に上げないもの」「別ファイルの値で置き換えるもの」を持ち、
`assemble.py` が適用する。適用結果は `raw/CURATION.md` に出る。

`TODO_REMEASURE.md` は残す。散文（何が起きたか、どう測り直すか）は
yaml に収まらないし、`curation.yaml` だけだと理由の分からない除外になる。
片方だけだと「理由の分からない除外」か「誰も守らない約束」のどちらかになる。

### 除外を2種類に分けた

`assemble.py` が自前で持つ除外（nb が1点だけのプローブ、より広い走査の
部分集合になっている周）は**データの形から決まる**。同じ入力なら同じ結果に
なるのでコードに書ける。

`curation.yaml` が持つのは、データを見ても分からない判断だけ。
サーマルスロットリング、スレッド数の取り違え、後日の手動再計測。
混ぜると「なぜこの条件だけコードに if があるのか」が読めなくなる。

### match のキーを間違えたら止める

`kind` を `kinds` と書き間違えたルールが黙って全件を除外するのが一番こわい。
未知のキーと空の `match` は読み込み時にエラーにする。
1件も当たらなかったルールは「未使用」として必ず報告する
（再計測が済んだのにルールを消し忘れると、新しい計測が黙って除外され続ける）。

### `--clean-sweep` が消してはいけないものを消していた

`rm -rf raw/qr_sweep` してから書き直す実装だったが、`raw/qr_sweep` には
assemble の管轄でないものが混ざっている。`ryzen7-5800x_s1_smt-on` は
学部時代のファイルを `migrate.py` が成形したもので、元ファイル
（`Ryzen7-5800X_16_2048_trial1.csv` 等）は `SRC_RE` に合わず assemble は読まない。
つまり **`make assemble` はこの構成のデータを消して、二度と作り直さなかった。**

「今回 `raw_data` を読んで出力先が決まったディレクトリ」だけを自分の持ち物と
みなし、その中の見覚えのない CSV を消す方式に変えた。curation で全トライアルが
落ちた条件（i5-7400）も持ち物に入るので、除外したデータはちゃんと消える。
読まなかった構成には触らない。

### i5-7400 を除外した

| 種別 | 理由 |
|---|---|
| `qr_sweep` | 2026-06-24 分がシングルチャネルで計測されていた（`studies/i5-7400_memory_channel/`）。デュアル化の効果が nb 依存（nb=32 で +5.9%、nb=448 で +21.4%）なので後から補正できず、最良 nb も 232→320 と動く |
| `ssrfb` | `threads=4` で計測。他の全ノードは `threads=1` で、th4/th1 が nb=64 で 0.92、nb=384 で 3.09 と曲線の形が別物になる |

`plan.yaml` の ssrfb `i5-7400` が `threads: 4` になっていたのは、この計測に
合わせて後から書いたもの。**計画側が間違っていた**ので `threads: 1` に直した。

`TODO_REMEASURE.md` が挙げる無効データ4件のうち、`curation.yaml` に入れたのは
この2件だけ。`i3-7100`（サーマルドリフト）と `i3-8100`（原因未特定）は
まだ `raw/` に残っている。特に i3-8100 は、原因が分からないまま除外すると
「何を直せば戻せるか」が残らないので、扱いを決めてからルールにする。

### 進捗表に「計測中」を足した（running.yaml）

進捗表は「計画にあるか」「データがあるか」の2値しか持たないので、
「まだ流していない」「流したがまだ終わっていない」「打ち忘れた」
「ジョブが落ちた」が全部おなじ `—` に見えていた。size16384 や AOBA の
バッチジョブは数日かかるので、この区別がつかないと同じ計測を二重に投入するか、
落ちたジョブを放置することになる。

`running.yaml` に書いた条件は進捗表に `▶` が付き、「計測中」の節に
**実際に打ったコマンド**が出る。データが `raw/` に入ったのに running のままなら
`make validate` が警告する。

`plan.yaml` と分けたのは、あちらが COVERAGE の分母を決めるレビュー対象の
ファイルだから。同じファイルに混ぜると「計測を1本流した」コミットと
「計画の分母を変えた」コミットが見分けられなくなる。

### cp932 コンソール対策を1箇所にまとめた

`assemble.py` は連結メッセージの `—` で、Windows のコンソールから走らせると
`UnicodeEncodeError` で落ちていた。しかも落ちるのは処理が全部終わって print する段
なので、「動いたのに最後だけ失敗する」という分かりにくい壊れ方をしていた。

対策は `sync_generated.py` が既に持っていたが、その場に貼ったコードだったので
他のスクリプトには波及していなかった。`tileqr_data.console.use_utf8()` に集約し、
各スクリプトから呼ぶ。

---

## 0.5.1 — ssrfb の反復目標を 5 から 1 に下げた

### なぜ defaults の trials: 5 を継承させないか

`plan.yaml` の `defaults.trials: 5` は、qr_sweep のように1回の計測ファイルが
nb×ib 格子1周ぶんの生データで、95% 性能帯が反復なしだと単点に潰れる
条件が出ることへの対策だった。

ssrfb はこの前提が成り立たない。**計測ツール自体が1回の呼び出しの中で
50回計測して平均を出している**ので、1ファイル（trials=1）の時点で
既に50サンプル分のノイズを均している。qr_sweep と同じ基準で trials: 5 を
求めると、実際には十分な反復が入っている計測が `partial` のまま止まる。

`ssrfb.trials` を 1 で明示的に上書きし、`make ingest` で COVERAGE.md を
再生成した（完了 40→49 / 78 条件、反復不足 15→5 / 56 条件）。

---

## 0.5.0 — Kurzak (2008) との照合を再現可能にした

### なぜ `src/tileqr_data/kurzak.py` を置いたか

「Kurzak の指数と本研究の指数のどちらが実測に合うか」は図2枚
（`fig_kurzak_p_exponent` / `fig_kurzak_size_exponent`）と
`scripts/kurzak_verify.py` の共通の土台になる。図ごとにフィットを書き直すと、
片方だけ直したときに指数が食い違い、**発表スライドと本文で違う数字が出る**。

`scripts/kurzak_verify.py` は `derived/` を書き換えない。これは検証レポートで
あってパイプラインではない。`make ingest` の対象に入れていないのはそのため。

### beta は構成ではなくアーキテクチャごとに持つ

`aoba-b_s1_smt-off`（64コア）と `aoba-b_s2_smt-off`（128コア）は同じ EPYC 7702
なので、カーネル効率 beta は同一でなければならない。ここを構成ごとに分けると、
データ中で**唯一「同一CPU・コア数だけ違う」対比**が beta に吸われ、
並列項のコア数指数が同定できなくなる。実際に分けて解いた版では
aoba の beta が 4700 まで発散し、コア数指数が 0.5 と 1.1 の間で揺れた。

### 自由 beta に上限 500 を置いた

SSRFB 実測から得た beta は 77〜343（epyc / i5-8500 / ryzen5-7400f）。
これを大きく超える値は物理ではなくフィットの逃げ場なので、既定で 500 で止める。
上限なしでも指数の結論は変わらないが、beta が発散した機種の解釈ができなくなる。

### scipy を依存に追加

非線形最小二乗（`scipy.optimize.least_squares`）が要る。
図の生成にしか使わないが、`figures` ターゲットが動かないと困るので本体依存にした。

---

## 0.4.0 — COVERAGE.md を計画の進捗から読ませる

### 表から node を落とした

AOBA はジョブごとにノードが変わるので、node で割ると同じ条件が5行に散り、
全体像が見えなくなる。表示の粒度は `(config, threads, size)` にした。
ノードの個体差は「最適 nb のばらつき」の節だけが受け持ち、
ノード単位の数値は `derived/optima.csv` に残してある。

### 計画を「どの CPU でも同じ」に統一

size 1024/2048/4096/8192/16384 の5種、nb 32-512、各5反復。
先頭に 機材 × size の進捗マトリクスを置き、全体像をそこで掴めるようにした。

### ib の刻みは nb の刻みに追随する

size ごとに nb の刻みを変える（4096 までが 4、それ以上が 8）が、
**ib の刻みも一緒に粗くなる**。実測がそうなっている:

| size | nb 刻み | ib 刻み | 1トライアルの点数 |
|---|---|---|---|
| 4096 | 4 | 4 | 3963 |
| 8192 | 8 | 8 | 1022 |

`ib_step` を 4 に固定して数えると size 8192 の期待点数が 2013 になり、
完走している計測が「48% しか埋まっていない」と表示される。
計画の分母を間違えると、**足りているものを足りないと報告する**方向に転ぶので
気づきにくい。`plan.yaml` の `ib_step_follows_nb_step` で表現している。

---

## 0.3.0 — raw_data の取り込みと1ファイル1トライアル化

計測13ファイル → 199ファイル。10機種・5サイズ。`ssrfb` を第3の測定種別として追加。

### `t{N}` はスレッド数ではなくトライアル数だった

旧命名 `..._t5_...` の `t` は、グリッドを**何周したか**だった。
`t5` のファイルは同じ `(nb,ib)` の組を5回ずつ持っている。
旧 `t1` ファイルが「同一条件の重複」に見えていたのは、実際には
64スレッドと128スレッドの別条件だったためで、原因はこの読み違いにある。

新命名では `t` をスレッド数に使い、周回は末尾の `_r{k}` で表す。

### 分割は行数を周回数で割らない

> 分割は行数を周回数で割っていません。`(nb,ib)` の再出現で境界を検出しています。
> epyc size2048 は 19,628行 = 5×3963 にならず、5周目が nb=500 で切れているためです。
> 等分割だと全周が壊れます。

途中で切れたファイルは例外ではなく普通にある。等分割は、
1本でも欠けた瞬間にそのファイルの**全周**の境界をずらす壊れ方をするので、
「たまたま今のデータでは合っている」実装になりやすい。採ってはいけない。

### 連結は物理ノードをまたぐ

AOBA はバッチ計算機なので、走査を再開したジョブには別のノード集合が割り当たる。
size16384 の nb 24-640 は3つのセグメントに分かれ、**3つとも別のノード集合**で
計測されている。重複する nb 416-424 でノード間に**中央値 2.7%** の差がある。
95% 帯を論じる尺度と同程度なので、無視できる差ではない。

規則: 重複 nb では **nb 区間が広い方**を採り、出来上がったファイルの `node` も
その側にする。どのファイルが1本になったかは `raw/JOINS.md` が持つ
（CSV の列には出所を書く場所が無い）。

**より広い走査の部分集合になっている周は連結しない。** 切れた周を
完走した周に混ぜることになるため、単独の部分トライアルとして残す。

### 反復は平均で畳む。最大ではない

反復が入った状態で `idxmax` を掛けると「5回のうち一番高く出た回」を拾う。
ノイズを均すどころか上振れを選び取ることになり、
95% 帯が単点に潰れる事象はこれで**悪化する**。平均してから最適値を取る。

効果は数字に出た。

| | 条件数 | 帯が単点 | ノイズ/帯 中央値 |
|---|---|---|---|
| trials ≥ 5 | 30 | 1 | 0.089 |
| trials = 1 | 28 | 4 | — |

残る退化4件はすべて AOBA（trials=1）。**AOBA の最適 nb を主張するなら反復が要る。**

### ファイル名の nb 範囲は信用しない

実データが名前より狭いことがある。
`i5-8500_ssrfb_size4096_nb32-768_...` の中身は nb 516-768 だけ、
`par006_size16384_nb24-512_...` は nb 424 で切れている。
`assemble.py` も `validate.py` も実データの範囲を採る。

### 出所が怪しいまま取り込んだもの

`epyc_size4096_..._20250101-010102` 〜 `-010105` の4件は、
2025年1月1日 01:01:02〜05（1秒間隔）というタイムスタンプ。
size4096 のスイープが1秒で終わるはずはないので、計測機の時計が
未設定だったかプレースホルダとみられる。データ自体は正常（各3963行）なので
取り込んでいるが、**この4件の計測日時は根拠にできない。**

---

## 0.2.0 — 計画に対する被覆と再現性の報告

`COVERAGE.md` を「何を測ったか」から
「計画に対してどこが空いているか」「その数字を主張に使ってよいか」に作り替えた。

### ib の上限規則は測定種別で違う

| 種別 | ib |
|---|---|
| `qr_sweep`, `ssrfb` | `ib <= nb/2` |
| `kernel_dtsmqr` | `ib <= nb - step` |

取り違えると計測点の期待値が**2倍ずれる**。数え方は
`src/tileqr_data/plan.py` の1箇所だけが持つ。

この算術は plasma-perf 側と一致する。nb 32-512 を qr_sweep の規則で数えると
**3963点**になり、向こうの `coverage` 列の分母と完全に一致した。

### machines.yaml とダッシュボード設定の食い違い（実害2件）

- **`calc` の CPU が別物だった。** `epyc-7543`（AMD 32コア、L3 は CCX 共有）と
  書いてあったが、`sources.toml` では `xeon_silver_4214`
  （Intel 2ソケット24コア、L3 はソケット共有）。
- **`epyc-7543` の L3 が半分だった。** `cores_per_ccx: 8` だと
  32/8 × 32MB = 128MB になり、`cpus.toml` の 256MB と食い違う。
  7543 は CCD あたり4コアのみ有効なので `cores_per_ccx: 4` が正しい。

どちらも `l3_sharing_unit` = shared-tile cache model の `C_unit` に直結する。
放置するとモデルの当てはめが狂う。`validate.py` が L3 の算術を検査するようにした。

### node 名はハイフン、source_key はアンダースコア

`sources.toml` のキーは `i3_6100` だが、node 名は `i3-6100` でなければならない。
ファイル名の node 部分が `[A-Za-z0-9-]+` に限られているため（`io.FILENAME_RE`）。
`validate.py` がこの取り違えを弾く。

---

## 0.1.0 — 統合版リポジトリ

`tileQR_data` と `tileQR_figures` を統合。データと図が同じコミットに入るので、
出典の記録（`data_commit()`）とパス解決（`TILEQR_DATA` 環境変数）が不要になった。
リポジトリをまたぐための配管が丸ごと消えている。
