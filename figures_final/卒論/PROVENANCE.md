# 卒論 図の生成情報

このディレクトリに図を置いたら、必ずこの表を埋めること。
**空欄のまま図だけコミットしない。** 数か月後に「この図はどのデータから
作ったか」を辿れなくなり、図を作り直せなくなる。

記入元は `figures/*/​*.png.meta.json`（`generate_figures.py` が出す
サイドカー）。入力ハッシュ・試行数・集約方法・各 size の `nb*` と
バンド範囲がそこに入っている。

## 生成環境

- 生成日：（YYYY-MM-DD）
- リポジトリ：tileQR_data
- コミットハッシュ：（`git rev-parse HEAD` の出力）
- Python環境：（`uv.lock` のハッシュ、または `uv pip freeze` の要約）

## 入力データ

- `derived/qr_sweep.parquet`（更新日：）
- `derived/ssrfb.parquet`（更新日：）
- `derived/optima.csv`（更新日：）
- `machines.yaml`（更新日：）

## 採用した集約方法

- （`median` / `mean` のどちらか。**必ず記載する**）
- 選んだ理由：（`figures/comparison/aggregator_comparison.md` の何を見て決めたか）

集約方法が違うと `nb*` が変わりうる。実測では 57 条件中 15 条件で変化し、
最大で 40 ずれた（`aoba-b_s2_smt-off` size8192、276 → 316）。
どちらで作った図かが分からないと、本文の数値と図が食い違う。

## 除外したデータ

`curation.yaml` で `raw/` に上げていない条件は、この図にも入っていない。
発表時点で除外されていたものを記録する（あとから復活した場合に
「あの図は除外前か後か」を判別するため）。

- （例：`i5-7400_s1_smt-off` qr_sweep 全サイズ — シングルチャネル計測のため）

## 図一覧

| ファイル名 | 内容 | 生成コマンド |
|---|---|---|
| | | |

## 備考

（データ除外の判断、パラメータの固定値、`band_threshold` を既定から
変えた場合など、図から読み取れない情報を記録）
