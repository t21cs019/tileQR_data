# 計測計画と COVERAGE.md

`README.md` の構成図から移設。

---

## いま流している計測（spec/running.yaml）

進捗表は「計画にあるか」「データがあるか」の2値しか持たないので、
そのままだと次の4つが全部おなじ `—` に見える。

- まだ流していない
- 流したが数日かかるのでまだ終わっていない
- 流したつもりでコマンドを打ち忘れていた
- 流したがジョブが落ちていた

size16384 や AOBA のバッチジョブは数日かかるため、この区別がつかないと
同じ計測を二重に投入するか、落ちたジョブを放置することになる。

```yaml
running:
  - kind: qr_sweep
    config: i5-7400_s1_smt-off
    threads: 4
    sizes: [1024, 2048]
    since: 2026-09-01
    host: i5-7400
    command: |
      ./run_sweep.sh --size 1024 --nb 32-512 --trials 5
```

1. 計測コマンドを打ったら1エントリ足して push
2. データが `raw/` に入ったらエントリを消す

`docs/COVERAGE.md` の進捗表に `▶` が付き、「計測中」の節に打ったコマンドが出る。
消し忘れ（データがあるのに running のまま）は `make validate` が警告する。

`command` は実際に打ったものをそのまま貼る。「どこまで打ったか」を後から
思い出すのが目的なので、清書しないこと。

**`spec/plan.yaml` と分けてあるのは**、あちらが COVERAGE の分母を決める
レビュー対象のファイルだから。同じファイルに混ぜると「計測を1本流した」
コミットと「計画の分母を変えた」コミットが見分けられなくなる。

---

## 最適 nb の定義はここだけが持つ

`scripts/ingest.py` が `derived/optima.csv` を生成する。
**図もダッシュボードも `derived/` を読むだけ**にすること。

3者（bench の確認プロット、dashboard、figures）が本当に重複しやすいのは
描画ではなく「全 sweep を読んで最適 nb を求める」部分。ここが複数箇所にあると、
片方だけ直したときに数値が食い違う。研究の主張そのものが最適 nb なので危険。

定義を変えたいときは `ingest.py` を直す。それだけで全部に伝播する。

### 反復は「平均」で畳む。最大ではない

`ingest.py` は最適 nb を求める前に、同一 (条件, nb, ib) の反復を**平均**する。

反復が入った状態でいきなり `idxmax` を掛けると「5回のうち一番高く出た回」を
拾うことになり、ノイズを均すどころか上振れを選び取る。95% 帯が単点に潰れる
事象はこれで悪化する。実際、反復を入れると帯の健全性ははっきり改善する。

| | 条件数 | 帯が単点 | ノイズ/帯 中央値 |
|---|---|---|---|
| trials ≥ 5 | 30 | 1 | 0.089 |
| trials = 1 | 28 | 4 | — |

`optima.csv` の列:

| 列 | 意味 |
|---|---|
| `nb_opt`, `ib_opt` | ピークを与える nb, ib（反復平均後） |
| `GFlops_max` | ピーク性能（反復平均） |
| `GFlops_sd` | ピークにおける反復間の標準偏差 |
| `noise_ratio` | `GFlops_sd / (0.05 × GFlops_max)`。1 に近いほど帯の狭さがノイズで説明できてしまう |
| `trials` | ピークに寄与した反復数 |
| `nb_lo95`, `nb_hi95` | ピークの 95% 以上を満たす nb の**連続区間** |
| `nb_in_band` | 帯に入る nb の**点数**。1 ならピークはノイズの棘 |
| `nb_scanned_lo/hi`, `n_nb` | 走査範囲（帯が範囲端で切れていないかの確認用） |

---

## 計測計画（spec/plan.yaml）と docs/COVERAGE.md

「何を測ったか」ではなく **「計画に対してどこが空いているか」** と
**「その数字を主張に使ってよいか」** を出すのが COVERAGE.md の役目。
前者が格子と trials、後者が 95% 帯の幅。

### 計画

どの CPU でも同じ。**size 1024 / 2048 / 4096 / 8192 / 16384 の5種、nb 32-512、各5反復。**
10 構成 × 5 サイズ = 50 条件（他の種別を含めて 69 条件）。

nb の刻みは size で変える。大きい行列は1点あたりの計測時間が延びるため。

| size | nb 刻み | ib 刻み |
|---|---|---|
| 1024 / 2048 / 4096 | 4 | 4 |
| 8192 / 16384 | 8 | 8 |

**刻みを粗くするとき ib も一緒に粗くなる。** `ib_step` を 4 に固定して数えると、
size 8192 以上で期待点数が倍近く過大になり、完走した計測が未達に見える
（`spec/plan.yaml` の `ib_step_follows_nb_step`）。

### 計測点の数え方

`spec/plan.yaml` の `grid` が nb×ib の格子を定義する。**ib の上限規則は測定種別で違う**。

| 種別 | ib の上限 |
|---|---|
| `qr_sweep`, `ssrfb` | `ib <= nb/2` (`half_nb`) |
| `kernel_dtsmqr` | `ib <= nb - step` (`nb_minus_step`) |

取り違えると期待点数が倍ずれる。数え方は `src/tileqr_data/plan.py` だけが持つ。

この算術は plasma-perf 側の `coverage` 列と一致する。
（例: nb 32-512 の qr_sweep は両方とも 3963 点になる）

### COVERAGE.md が出すもの

**先頭が「計画の進捗」**。機材 × size のマトリクスで、セルは
「取れている反復数 / 計画の反復数」。全体像はここだけ見れば分かる。

| 記号 | 意味 |
|---|---|
| `5/5` | 計画どおり |
| `1/5` | 反復が足りない |
| `—` | 未計測 |
| `·` | 計画に無い |
| `!` | 反復は足りているが nb が計画の範囲に届いていない |
| `▶` | 計測中（`spec/running.yaml` に載っている） |
| `x` | これ以上データが来ない（`spec/plan.yaml` の `frozen`） |

次が「計測中」。`spec/running.yaml` の内容と、そのとき打ったコマンドが出る。

その次が「これ以上データが来ない条件」。計測機材が手を離れたなどの理由で、
計画に届かないまま確定した条件と、その理由。`spec/plan.yaml` の target に
`frozen: <理由>` を書くと、進捗表に `x` が付き**達成率の分母から外れる**
（埋まらないものを分母に残すと 100% にならず、指標として見なくなるため）。

`trials` を実績値に下げて done 扱いにはしないこと。反復が1本しか無い
`nb_opt` が主張に使えないことは変わらず、それを表から消してしまう。

以下は詳細。

- **条件別** … nb 走査範囲・刻み・ib 範囲・計測点・格子の充填率・計画達成率・trials・status
- **最適 nb のばらつき** … 独立測定が2つ以上ある条件だけ。幅と変動係数
- **95% 性能帯の健全性** … 帯が単点に潰れた測定を「退化 n/m」として名指し
- **machines.yaml との整合** … 未計測ノード、未定義ノード、交絡要因が unknown の構成

### 表に node を出さない理由

AOBA はバッチ計算機でジョブごとにノードが変わるので、node で割ると
同じ条件が5行に散って全体像が見えなくなる。読み手が知りたいのは
「その条件が何本取れているか」で、それはノードをまたいだ本数の合計。

ノードの個体差は「最適 nb のばらつき」の節が受け持つ。
ノード単位の数値が要るときは `derived/optima.csv` に残してある。

### trials（反復回数）を計画に入れてある理由

128 スレッド系で 95% 性能帯が単点に潰れる条件が出ている。
ピークが「なだらかな山の頂上」ではなく「ノイズの中で偶然突き出した棘」で、
この状態の `nb_opt` は再現性がない。同一条件を反復して平均を取るまで、
その値は研究の主張に使えない。`spec/plan.yaml` の `trials` がその目標値で、
達成できていない条件は COVERAGE.md のサマリに件数が出る。
